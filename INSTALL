# To install backend dependencies run 'startup.sh' in the backend folder
./startup.sh
# To spin up the backend server, run 'run.sh'
./run.sh

# In another terminal window, we want to setup the frontend, enure you have nodejs and npm installed
# Navigate to the frontend directory

# Run
npm install

# Start
npm start




